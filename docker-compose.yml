services:
  verisim:
    build: .
    ports:
      - "3077:3077"
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - PORT=3077
    volumes:
      - ./prisma/prisma:/app/prisma/prisma
    restart: unless-stopped
